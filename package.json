{
  "name": "@learning",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "build": "pnpm build:backend && pnpm build:shared && pnpm build:frontend",
    "build:backend": "pnpm --filter backend build",
    "build:shared": "pnpm --filter shared build",
    "build:frontend": "pnpm --filter frontend build"
  },
  "devDependencies": {
  }
}